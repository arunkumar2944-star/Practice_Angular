export enum UserType {
  Admin,
  User,
}

export enum Category{
  Personal,
  Work,
  Study,
  Meeting
}

export enum Priority{
  Low = 'Low',
    Medium = 'Medium',
    High = 'High'
}

export enum Status{
  Draft,
  Active,
  Completed,
  Archived
}

export enum Visibility{
  Private,
  Team,
  Public
}
